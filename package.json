{
  "name": "semasignalbot",
  "version": "1.0.0",
  "description": "StockCrypto PRO - Telegram Web App",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-speed-insights.js",
    "lint": "echo 'No linter configured'",
    "dev": "python3 -m http.server 8000"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.12"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
